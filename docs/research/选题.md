# 缺失历史轨迹条件下行人未来运动的结构化多模态预测

_研究框架记录 · 更新时间：2026-09-16 · 研究领域：行人轨迹预测；基础建模框架：DeMo；数据基准来源：TrajImpute_

> **文档状态（2026-09-16）**：本文档为唯一选题文档。**方法层已清空、待重新构思**（历史方法版本不保留，git 历史可溯）；M0 为唯一滚动基线（三环境全测，见 §八.1）。主链 K=20（§1.6）。

## 一、选题定位

行人轨迹预测通常以检测与跟踪系统输出的历史轨迹为输入。然而，遮挡、漏检、短时跟踪中断、帧丢失与定位误差会使历史轨迹出现缺失或时间断裂。完整、连续的固定长度历史轨迹因此并不是始终可得的前提。

本选题研究**缺失历史轨迹条件下的行人多模态轨迹预测**：给定不完整的历史观测、缺失位置及其时间结构，直接预测行人的完整未来轨迹分布。研究目标不是将缺失帧逐点恢复为唯一历史，而是从可见轨迹片段和观测缺失结构中构造可用于未来预测的历史上下文。

DeMo 为本研究提供结构化多模态预测的基础框架。其核心思想可表述为：未来轨迹分布同时包含两类结构，一类是不同候选未来之间的整体差异，另一类是单条候选轨迹沿时间轴的动态演化。DeMo 分别表示这两类结构，并通过联合表征参数化完整的未来轨迹分布[^1]。本研究将这一结构化预测思想引入缺失历史行人轨迹预测，使历史观测缺失不仅作为输入扰动，而是作为条件未来分布建模的一部分。

核心科学问题是：

> **在行人历史轨迹因观测中断而不完整的条件下，如何将可见轨迹片段与缺失结构转化为有效的预测条件，并对未来轨迹分布中的跨候选差异和轨迹内时序演化进行联合建模，从而生成多模态且时间连续的未来轨迹？**

研究定位分为五个层次：

| 层次 | 内容 | 在选题中的作用 |
| --- | --- | --- |
| 现实背景 | 遮挡、漏检、跟踪中断等导致历史轨迹缺失 | 给出问题来源和应用需求 |
| 预测任务 | 不完整历史观测条件下的行人未来轨迹预测 | 定义输入、输出与评测对象 |
| 历史建模 | 观测缺失感知的时序历史表征 | 从可见片段、缺失掩码和时间间隔中构造预测上下文 |
| 未来建模 | 跨候选差异与轨迹内时序演化的结构化条件建模 | 保持多模态性与时间连贯性 |
| 结果验证 | 完整未来轨迹上的准确性、覆盖性、连续性和缺失鲁棒性 | 判断方法是否解决任务问题 |

整体研究链为：

```text
检测/跟踪观测缺陷
    -> 缺失或时间断裂的历史轨迹 X^obs
    -> 观测掩码 M、时间间隔 Delta 与有效运动片段
    -> 共享骨干上下文 H^base（M0）
    -> 缺失观测条件化上下文 H^miss
    -> 未来轨迹的跨候选差异表征 D_1:K
       + 轨迹内时序演化表征 S_1:T_f
    -> 结构化条件未来分布 p(Y_1:T_f | X^obs, M, Delta, C)
    -> 多模态行人未来轨迹预测
```

本选题的核心不是将某一种时序网络本身包装为创新，也不是把编码器隐式特征直接解释为真实“运动意图”或“物理状态”。整体编码器负责聚合历史时序证据、观测结构和行人交互信息；DeMo 的 Mode/State 结构提供未来分布的结构化参数化。为避免基础模型能力不足掩盖缺失感知机制，研究路线分为“共享骨干”与“缺失感知方法层”两层：前者为经过三环境验证的 M0 基线，后者研究缺失观测如何改变历史表征与结构化未来分布。研究创新仍应落在**缺失历史任务建模与缺失条件如何进入未来分布建模**，而不是简单叠加通用网络组件。

### 1.1 共享骨干与方法层版本

当前 DeMo 与公开强基线之间仍存在绝对性能差距，因此不能把所有后续提升都归因于缺失感知设计。M0 为共享骨干与滚动基线：所有方法层版本与 M0 共用相同模型结构、优化配置与评测口径，只在训练目标或训练分布上做单变量增量。这样可以分别回答两个问题：

1. M0 在零缺失条件下能否提供稳定的预测能力（已由 Clean 基线回答）；
2. 在相同骨干上，显式建模缺失过程是否仍能带来独立收益。

模型版本统一定义如下：

| 版本 | 定义 | 研究定位 |
| --- | --- | --- |
| `M0` | 原始 DeMo + 掩码拼输入（Clean 0.232/0.389 ｜ Easy-direct 0.245/0.399 ｜ Hard-direct 0.432/0.640） | 滚动基线与共享骨干 |

方法层当前无候选版本（2026-09-16 清空）：后续版本与 M0 共用相同模型结构、优化配置与评测口径，只在训练目标、输入特征或训练分布上做单变量增量。

任何版本通过确认性门槛后更新滚动基线；未通过则整体删除，不进入后续组合。

## 二、问题定义与概念体系

### 1. 缺失历史轨迹预测任务

记完整历史轨迹为 `X_1:T_h`，未来轨迹为 `Y_1:T_f`，历史观测掩码为 `M_1:T_h`。其中，`M_t = 1` 表示第 `t` 个历史时刻具有有效观测，`M_t = 0` 表示该时刻的轨迹观测缺失。实际输入可记为：

> **公式：** `X^obs_t = M_t ⊙ X_t`

除位置观测外，模型还可以使用由观测过程导出的时间信息：

```text
M_t       : 当前时刻是否存在有效观测
Delta_t   : 距离最近一次有效观测的时间间隔
E_t       : 由足够有效观测计算得到的运动证据，例如位移或速度
C         : 行人交互上下文
```

任务目标是建模：

> **公式：** `p_theta(Y_1:T_f | X^obs_1:T_h, M_1:T_h, Delta_1:T_h, C)`

未来轨迹在训练和评测中保持完整，缺失只发生在输入历史窗口。轨迹插补可以作为对照方法或可选辅助，但不是本选题的主目标；主目标始终是**缺失历史条件下的未来预测质量**。

### 1.2 误差结构分解与改进依据

预测性能随缺失退化的误差来源与当前覆盖设计：

| 层 | 误差来源 | 覆盖设计 |
| --- | --- | --- |
| ① | 编码器不知道哪些帧可信 | M0 已内置（掩码拼输入）；不再单独设版本 |
| ③ | **证据塌缩**：缺失越大，模态概率越自信、分布越窄，MR 恶化 | 待构思 |
| 泛化层 | 训练缺失分布（Easy 0–4 帧）与高缺失测试分布（Hard 4–7 帧）不匹配 | 待构思 |

关键事实基础：Easy-direct 按缺失等级分组显示，MR 的恶化速度显著超过 minFDE（如 ETH 场景 m=0→m=4：MR 约 +50% vs minFDE 约 +9%），表明 best-of-K 之外的概率质量退化是缺失条件下未被现有 direct 方法处理的主要误差源；插补路线（MS-TIP/GC-VRNN/BRITS）不改变预测器的概率头，同样未覆盖此层。误差层③与泛化层是方法层重构时的候选切入点（诊断事实保留，机制假设重新构思）。

### 2. 缺失历史的现实含义与实验抽象

本选题中的“缺失历史轨迹”指历史窗口内部分时刻没有可用的行人位置观测，或观测序列在时间上不连续。它对应以下真实观测现象：

```text
遮挡或视野离开              -> 短时位置不可见
检测漏检或帧丢失            -> 离散时间步缺失
跟踪中断或重新关联          -> 连续历史片段断裂
定位质量下降                -> 后续可扩展为噪声与缺失共存
```

第一版以**历史帧缺失与连续断裂**为可控研究对象。随机缺失、连续缺失、不同缺失比例和不同缺口位置是构造任务难度与模拟观测中断的实验条件，而不是彼此割裂的研究主题。若后续加入坐标噪声、误检或身份切换，应将其作为扩展的观测退化模型，而不是提前并入当前结论。

### 3. 未来轨迹的结构化条件分布

普通多模态预测通常直接从历史上下文产生 `K` 条候选未来。结构化未来建模则进一步规定未来分布的组织方式：未来不是若干条彼此独立的完整轨迹，而是由“候选未来之间的差异”和“单条候选内部的时序演化”两类关系共同决定。前者负责区分不同的未来分支，后者负责组织每条轨迹随时间的变化。

这里的“结构化”指：

```text
不是一个隐藏向量直接对应一条完整轨迹；
而是先表示未来候选之间的差异结构，
再表示单条未来沿时间轴的演化结构，
最后对两类结构进行联合参数化。
```

记：

```text
D_k       : 第 k 个候选未来的跨候选差异表征
S_1:T_f   : 沿预测时间演化的轨迹内时序表征
Q_k,1:T_f : D_k 与 S_1:T_f 的联合未来表征
```

则可将未来轨迹分布理解为：

> **公式：** `p_theta(Y | H^miss, C) = sum_k pi_k p_theta(Y | Q_k,1:T_f, H^miss, C)`

其中：

> **公式：** `D_1:K = f_cross(H^miss, C)`

> **公式：** `S_1:T_f = f_time(H^miss, C)`

> **公式：** `Q_k,t = Phi(D_k, S_t, H^miss, C)`

因此，结构化未来建模可以写成：

```text
历史条件 H
    -> 跨候选差异表征 D_1:K
    -> 轨迹内时序演化表征 S_1:T_f
    -> 联合未来表征 Q_1:K,1:T_f
    -> 多模态未来轨迹分布
```

这是一种对未来轨迹分布的**分解表示与联合参数化**。它不要求 `D_k` 或 `S_t` 对应某个可以直接观测的心理或物理变量。DeMo 原文中的 `Mode Query` 可对应跨候选差异表征，`State Query` 可对应轨迹内时序演化表征，`Hybrid Coupling` 则对应二者的联合参数化[^1]。

### 4. 核心术语

| 术语 | 本选题中的含义 |
| --- | --- |
| 缺失历史轨迹 | 具有缺失时间步或时间断裂的行人历史观测序列 |
| 缺失结构 | 掩码、缺失位置、连续缺口长度、观测间隔等观测过程信息 |
| 缺失感知历史表征 | 同时编码可见轨迹片段及其缺失结构的预测上下文，不将缺失值当作真实运动值 |
| 跨候选差异表征 | 描述多个合理未来分支之间的差异结构，对应 DeMo 的 Mode 维度 |
| 轨迹内时序演化表征 | 描述单条未来轨迹沿时间轴的变化结构，对应 DeMo 的 State 维度 |
| 联合未来表征 | 将跨候选差异与轨迹内演化联合起来，参数化具体未来轨迹 |
| 多模态未来预测 | 对同一历史观测给出多个可能未来分支及其概率或尺度 |
| 时空上下文编码器 | 对带缺失结构的历史轨迹、有效运动关系和行人交互进行联合编码 |

## 三、问题背景与研究动机

### 1. 完整历史假设与真实观测条件之间存在差距

现有行人轨迹预测通常在完整、连续且等间隔的历史轨迹上训练和评测。该设定有利于模型从位置差分中获得速度、方向和转向趋势，但与检测跟踪系统的实际输出存在差距：一旦历史中出现缺口，部分运动变化不再可直接计算，最近观测状态与预测起点之间也可能发生时间脱节。

因此，缺失历史不是单纯少了若干坐标，而是改变了模型可使用的历史时序证据及其时间组织方式。

### 2. 缺失值不能被视为普通轨迹值

对缺失位置进行零填充、前向填充或直接插补后再输入普通预测器，容易混淆以下信息：

```text
真实静止                != 位置缺失后的零位移
低速或减速              != 长时间没有新观测
连续转向趋势            != 被缺口切断后的局部片段
最近状态可见            != 最近状态距离预测起点较远
```

因而，历史编码需要显式区分“观察到的运动几何”和“观察过程本身”，使模型能够根据缺失位置与时间间隔判断哪些历史关系可用、哪些关系不能被当作确定事实。

### 3. 缺失历史会放大未来分布建模难度

行人未来本身具有多模态性：继续前行、转向、减速、停走与避让等运动结果可以共享相似的局部历史。历史发生缺失后，多条未来之间的区分证据进一步减少，普通的一轨迹一查询式预测容易出现两类问题：

```text
多条候选轨迹趋同，无法覆盖合理未来分支；
单条候选轨迹缺少时间连贯性，速度或转向变化不稳定。
```

因此，需要同时解决两个问题：一是从不完整历史中建立有效条件上下文，二是用适合多模态未来的结构化方式表示未来分布。

### 4. DeMo 为缺失历史预测提供的结构化基础

DeMo 将多模态未来轨迹的跨分支差异与轨迹内时间演化分开表示，再通过联合表征形成具体轨迹[^1]。这一思想适合被重新放置到缺失历史任务中：当历史证据存在断裂时，模型不应只依赖单一隐藏向量直接输出未来，而应在缺失条件下分别保持候选未来的差异性和单条轨迹的时间组织。

本研究的主张不是重新定义 DeMo 的原始 Mode/State 创新，而是提出：

> **缺失历史行人预测需要观测过程条件化的历史表征，并需要将该条件上下文贯穿到结构化多模态未来分布的构造中。**

## 四、核心科研问题与研究假设

### 1. 总问题

> **如何在缺失历史轨迹条件下，将可见轨迹片段、缺失结构和行人交互上下文转化为有效的预测条件，并以跨候选差异与轨迹内时序演化相结合的结构化方式建模未来轨迹分布？**

### 2. 子问题

1. **缺失感知历史表征**

   如何使历史编码器同时识别可见运动片段、缺失位置和观测间隔，避免将缺失误认为静止、低速或确定性运动？

2. **缺失条件下的跨候选未来建模**

   当历史证据不完整时，如何维持候选未来之间的有效差异，使模型能够覆盖多个合理的行人未来分支？

3. **缺失条件下的轨迹内时序建模**

   如何根据不连续历史建立单条未来轨迹的时间演化表征，保持未来速度、方向和转向过程的合理连续性？

4. **缺失结构与联合未来分布的适配**

   缺失位置、连续缺口和观测间隔应如何作用于跨候选差异表征、时序演化表征及其联合建模，使未来分布与历史观测条件相匹配？

5. **任务层有效性**

   相比直接填补缺失值、只拼接掩码或直接复用完整历史预测器，缺失感知的结构化未来分布建模能否在 ETH/UCY 与 SDD 上稳定提高缺失历史条件下的多模态预测质量？

### 3. 可检验假设

| 假设 | 内容 | 需要的证据 |
| --- | --- | --- |
| H2 | 缺失历史下，未来分布的跨候选差异与轨迹内时序演化需要分别建模，才能同时维持模态覆盖与轨迹连续性 | 与一体化预测头、未适配 DeMo 的对照 |
| H3 | 将缺失结构作为结构化未来分布的条件变量，能够减少候选轨迹塌缩并改善连续缺口场景中的预测 | 对比只在输入侧处理缺失与贯穿式条件化 |
| H4 | 本文方法在 Hard-direct 高缺失评估中仍保持收益，并在跨数据集扩展证据中方向一致 | Hard-direct 与 SDD 扩展验证 |

## 五、研究内容与方法框架

### 0A. 方法层状态（2026-09-16 清空）

方法层当前**无候选版本**（2026-09-16 清空）：历史方法版本不保留（git 历史可溯）。

保留下来的资产：

1. **M0 滚动基线**：三环境收口（Clean 0.232/0.389 ｜ Easy-direct 0.245/0.399 ｜ Hard-direct 0.432/0.640），作为一切后续对照的零点；
2. **误差结构分解（§1.2）**：编码器缺失感知（层①，M0 已内置）、证据塌缩（层③，诊断事实保留）、Easy→Hard 训练分布失配（泛化层）；
3. **验证框架（§八）**：官方 Easy/Hard direct 协议、三件套指标（minFDE20/b-minFDE20/MR）、性能/稳定性/归因三道门槛、Go/No-Go 规则。

方法层重构的约束：新候选必须以 M0 为唯一出发点、单变量增量、过确认性门槛才进主链；构思路径不写入本文档（单独记录）。

### 1. DeMo 与本研究的关系

| DeMo 原始设计 | 本研究中的科学作用 | 本研究的新增问题 |
| --- | --- | --- |
| Mode Queries | 表示不同候选未来之间的结构差异 | 缺失历史下如何生成足够分散且合理的候选未来 |
| State Queries | 表示未来轨迹沿时间轴的演化结构 | 不连续历史如何支持时间连贯的未来演化 |
| Mode Localization | 从历史与上下文中定位候选未来差异 | 缺失结构应如何进入候选未来的条件构造 |
| State Consistency | 组织单条未来内部的时序关系 | 观测间隔是否需要调节未来演化的条件信息 |
| Hybrid Coupling | 联合参数化跨候选差异与时序演化 | 缺失条件应如何参与联合未来表征 |
| Attention + Mamba | 聚合交互关系与时序证据 | 如何编码不连续历史中的有效片段与缺口结构 |

DeMo 是结构化未来预测的基础骨架；本研究的核心工作是使该骨架能够面向缺失历史观测构造有效的条件未来分布。

## 六、相关研究启示与研究空白

本节只保留正式发表、属于本领域主流高水平会议或具有明确直接参考价值的工作；通用性弱、发表层级不足或尚未正式发表的条目不纳入当前选题框架。

### 1. 缺失观测与不完整历史预测

| 研究 | 可借鉴内容 | 在本选题中的作用 |
| --- | --- | --- |
| Graph-VRNN [^4] | 部分观测下的多主体隐状态与预测不确定性 | 不完整观测下的条件未来预测参考 |
| Missing Pattern / GC-VRNN [^5] | 缺失模式、时间衰减和插补预测联合建模 | 时间断裂与预测关系的参考 |
| MS-TIP [^2] | 插补感知行人轨迹预测 | 直接预测与插补式处理的对照参考 |
| Missing Data Benchmark / TrajImpute [^3] | 缺失数据集构造、插补基准与统一评测 | Easy-direct/Hard-direct 的官方缺失数据来源；官方 Clean/Easy-impute/Hard-impute 仅作外部参考（§八.0） |
| Momentary Observation [^6] | 极短历史观测下的行人预测 | 观测不足时历史运动线索的边界参考 |
| DTO [^7] | 完整历史知识向少观测模型迁移 | 可选的完整历史教师或一致性对照 |
| BRITS [^14] | 双向递归缺失值插补 | 插补式历史预测的对照或辅助分支 |

这些工作说明缺失历史可以直接参与未来预测，也说明插补并不是唯一路线。对本研究而言，关键问题是如何将缺失观测条件与多模态未来分布的结构化建模连接起来。

### 2. 多模态未来与结构化轨迹表示

| 研究 | 可借鉴内容 | 在本选题中的作用 |
| --- | --- | --- |
| DeMo [^1] | 跨未来差异与时间演化的解耦表示及联合预测 | 结构化未来分布的主体框架 |
| MTR [^15] | 可学习运动查询与多模态未来定位 | 缺失条件化 Query 的设计参考 |
| MID [^8] | 运动不确定性与多模态轨迹生成 | 未来分布多样性与覆盖性参考 |
| SocialVAE [^9] | 逐时刻潜变量和社会注意力 | 时变未来结构与不确定性参考 |
| DisDis [^10] | 个体轨迹分布的区分性建模 | 多模态分布区分与评价参考 |
| SingularTrajectory [^11] | 连续低维轨迹表示 | 不依赖固定行为类别的表示参考 |
| Stop-and-Go Forecasting [^12] | 停走等局部运动变化 | 转折与速度变化场景的评估参考 |
| SocialCircle [^13] | 角度化社会交互表示 | 行人交互作为条件上下文的参考 |

### 3. 研究空白

上述高水平研究已经覆盖缺失数据处理、轨迹插补、少观测预测、社会交互和多模态生成等方向，但仍存在一个可聚焦的问题：

> **针对缺失历史行人轨迹，如何将观测缺失结构显式引入历史上下文与结构化未来分布建模，使模型在不完整输入下同时保持候选未来的覆盖性和单条轨迹的时间连贯性？**

本研究的差异化不在于声称首次研究缺失轨迹，也不在于重新命名 DeMo，而在于将以下两条已有研究线系统结合并进行任务级验证：

```text
缺失观测感知的历史时序建模
    +
多模态未来分布的跨候选/时序结构化参数化
    ->
缺失历史条件下的行人多模态轨迹预测
```

基础运动与交互增强属于支撑上述两条研究线的共享能力层，而不是第三条独立创新线。它们的作用是让比较从一个可靠的预测起点开始；真正需要在论文中重点论证的仍是：同一基础骨干在观测缺失后，是否能够通过显式观测过程条件化保持未来分支覆盖和轨迹内时间连贯性。

## 七、论文叙事与拟形成贡献

### 1. 论文主叙事

本文面向遮挡、漏检和跟踪中断导致的行人历史轨迹缺失问题。不同于假设完整历史可用的传统预测方法，本文在统一协议下以经过三环境验证的 M0 为共享骨干，将历史观测缺失视为条件预测中的观测过程变量：模型从可见轨迹片段、缺失掩码和时间间隔中构造缺失感知历史上下文，并基于结构化多模态未来分布建模，分别表示候选未来之间的差异和单条未来轨迹内部的时序演化，最终生成与不完整历史观测相匹配的多模态行人未来轨迹。缺失观测条件化才是本文的主要方法问题。

论文叙事应明确区分两种性能目标：`M0` 与外部强基线的比较反映模型的绝对预测竞争力；同一 `M0` 上方法层版本的配对比较反映该版本机制的独立价值（方法层待重构）。两者不能合并成一个无法归因的“整体提升”。

### 2. 拟形成的贡献

1. **任务建模贡献**

   将行人轨迹预测从“完整历史到未来”的标准设定扩展为“缺失历史观测到未来”的条件预测问题，以掩码、观测间隔和有效轨迹片段共同描述历史输入。

2. **结构化未来分布贡献**

   将缺失条件引入 DeMo 式跨候选差异表征、轨迹内时序演化表征及其联合参数化过程，使多模态未来生成与历史观测完整程度相适配（方法层待重构）。

3. **实证与诊断贡献**

   在 TrajImpute 官方 Easy/Hard release 上构造直接缺失预测评估（Clean 基线 = 本文 DeMo 在原始完整数据上的复现结果，暂定 0.232/0.389；原论文数字为外部参照）：系统比较直接预测、掩码感知、插补后预测外部参考和结构化未来分布建模，并分析官方缺失设置下的准确性、覆盖性、时间连续性和退化行为；SDD 旧自定义缺失结果仅作为历史预实验证据，不构成正式主结果。

### 3. 题目与方法名称

当前推荐题目：

> **缺失历史轨迹条件下行人未来运动的结构化多模态预测**

可选的更方法化表述：

> **基于观测缺失感知历史表征与结构化未来分布建模的行人轨迹预测**

推荐的方法总称：

> **观测过程条件化的结构化多模态轨迹预测**

### 4. 论文结构建议

| 章节 | 需要回答的内容 |
| --- | --- |
| Introduction | 真实观测缺陷为何使完整历史假设失效；本文的任务、关键思想和贡献是什么 |
| Related Work | 缺失轨迹预测、插补、少观测预测、行人多模态预测和结构化未来建模之间的关系 |
| Problem Formulation | 缺失历史观测、掩码、时间间隔、交互上下文和条件未来分布的形式化定义 |
| Method | 缺失感知历史表征、跨候选差异表征、轨迹内时序演化表征及其缺失条件化联合建模 |
| Experiments | 数据集与缺失协议、主结果、基线比较、消融、不同缺失模式和定性案例 |
| Analysis | 多模态覆盖、轨迹连续性、观测间隔敏感性和跨数据集泛化 |
| Conclusion | 缺失历史预测中的有效建模原则与局限 |

## 八、验证框架与实验推进

### 0. 正式 benchmark 与历史协议的关系（2026-09-07 二次修正）

TrajImpute 只作为**数据和评估基准来源**，不改变本文的研究问题、模型结构、创新点和方法主线。本选题不是“以 TrajImpute 为中心”，也不提出插补模型；本文方法仍是直接缺失感知的多模态未来轨迹预测。必须区分两类协议：

**TrajImpute 官方参考协议（插补后预测）**

```text
Clean        : 完整历史上的预测参考结果
Easy-impute  : 缺失历史先经外部插补器恢复、再输入预测器的参考结果
Hard-impute  : 高缺失历史先经外部插补器恢复、再输入预测器的参考结果
```

这三者是 TrajImpute 原论文/README 的预测参考协议，主要对应**插补后预测**结果，作为本文的**有限范围外部参考**，不是本文方法的原生 benchmark。

**本文直接缺失预测扩展协议（TrajImpute-derived direct missing-aware evaluation）**

```text
Clean        : 原始 ETH/UCY 零篡改（经典完整历史预测，MoFlow 官方 LOO 数据）。
               本文 Clean 基线 = 本项目 DeMo 复现结果，暂定 DeMo-单向 0.232/0.389
               （2026-08-28 轮，见《完整数据基线对比》）；原论文数字为外部参照。
Easy-direct  : 基于 TrajImpute release 发布的 Easy 缺失样本（0–4 帧，
               生成逻辑已由官方 generate_data.py 核验）构造的直接预测评估
               （不插补）：缺失历史 + missing mask + 有效观测间隔 + 场景信息
               直接输入本文模型
Hard-direct  : 直接使用已下载并通过审计的官方 release Hard pkl（每轨迹恰缺
               4–7 帧之一，官方生成后直接发布、生成脚本未公开，分布见 §八.0.2）
               构造的直接预测评估（不插补）——已确认为官方发布数据，
               属于 TrajImpute-derived 扩展评估
```

**工程验收状态（2026-09-08 修正）**：release 审计、30 个 pkl 的五场景适配扫描、直接预测评估器、M0 forward/backward 和真实数据分组评估均已在项目指定 DeMo 环境中通过；（2026-09-13 更正：自建 benchmark 与 Clean-direct runner 已移除。）`target_mask` 的 padding actor 语义已修正为无效，不会进入其他 actor 辅助损失。该验收只说明 direct 管线可运行，不构成任何模型性能结论。Easy 的零缺失子集仅保留为诊断用途。

若未来 release 不可得而需本地构造 4–7 帧缺失，则该结果只能称"TrajImpute-based 补充压力测试"，不与官方 Hard-impute 直接对照，也不进入 direct 主表。

**数据来源与“缺失样本”的谨慎表述**（官方事实核验状态见 §八.0.1，release 审计见 §八.0.2）：

- 正式实验数据来源 = TrajImpute 官方 release pkl（已下载并通过数据完整性审计，§八.0.2）+ 官方数据组织；缺失样本直接采用官方发布样本，实际使用的字段、文件路径、掩码和样本数量以 release 审计结果为准。

**历史协议归档与统一说明**

历史 v1/v2/v3 自定义协议及其全部结果**降级为“历史预实验 / 探索性证据”**：用于定位问题与指导设计，不与 Easy-direct/Hard-direct 主结果混合，也不从中推导新 benchmark 的预期提升百分比。统一标注：

> 这些结果来自历史自定义 v3 无保护、train_adapt、单种子探索性实验；其价值是定位问题和指导设计，不构成 TrajImpute 官方结果，也不构成本文 TrajImpute-derived 直接预测主结论。

连续缺失、末端缺失、锚点间隔等自定义维度仅作为**基于直接预测扩展数据的分组分析或补充分析**，不是 TrajImpute 原论文已定义的独立官方主协议。

### 0.1 TrajImpute 官方来源核验记录（2026-09-07）

仅依据官方仓库（github.com/Pranav-chib/TrajImpute）、官方 README 与论文（arXiv:2411.00174）核验：

已核验（逐条出处：README / `generate_data.py` 全文 / 论文 §1、§5）：

1. 场景：ETH、HOTEL、UNIV、ZARA1、ZARA2（缺失子集以 -M 后缀标注）；历史 8 帧、未来 12 帧（脚本 `total_values=8`、`obs_traj_rel` 取前 8 帧、`pred_traj` 12 帧）。
2. 官方原始数据组织：`generate_data.py` 读取并依赖 `obs_traj`、`pred_traj`、`pred_traj_rel`、`loss_mask`、`non_linear_ped`、`seq_start_end` 等字段，train/test/val 为三个独立 pkl，划分由 pkl 直接提供。release 实际字段、shape 与文件组织已由下载审计确认（2026-09-07，§八.0.2）：8 键字典与脚本依赖一致；release 保存于 `~/TrajImpute/dataset/TrajImpute/`，5 场景 × Easy/Hard × 3 split 共 30 个 pkl（约 238 MB）。
3. `generate_data.py` 输入输出：读三个原始 pkl → 对每轨迹从 {0,1,2,3,4} 均匀采样缺失帧数、`np.random.choice(8, k, replace=False)` 散布缺失位置、缺失值置 `np.nan` → 输出 `data_train/data_val/data_test.pkl`（新增 `missing_mask` 与 `obs_traj_rel`）；test 按每个缺失数复制 5 份（即官方缺失数为 0–4 的扩展测试集）。
4. Easy/Hard 缺失帧数范围：README 明确 Easy = 0–4 帧、Hard = 4–7 帧。**release 实测确认**（§八.0.2）：Easy train/val 每轨迹缺失 0–4 帧（计数近均匀，均值约 25%）；Hard 每轨迹缺失恰为 4–7 帧之一（直方图仅 4/5/6/7 有质量，均值约 68.7%）。`generate_data.py` 不含 Hard 生成逻辑，Hard 由官方生成后直接发布、生成脚本未公开。
5. 官方预测参考协议名称：README 使用 `Clean`、`Easy-impute`、`Hard-impute`，且预测基准表格明确说明是在**插补后的数据**上评估预测模型（论文 §5.2 “prediction methods on the imputed subsets”）。
6. 官方原生直接预测评估器：**README 与仓库均不提供**直接输入 NaN/缺失历史、由预测模型直接预测未来的评估器；`test.py` 是外部模型（EigenTrajectory 仓库风格）代码拷贝，依赖本仓库不存在的 `dataset.py`/`model.py`。ADE/FDE 由外部预测模型代码完成。
7. 官方发布形态：**已确认官方直接发布生成好的缺失数据**（Google Drive release，README 链接；已下载），无需自行运行 `generate_data.py` 生成正式实验数据。
8. **脚本专项风险与 release 裁定（2026-09-07 审计完成）**：`generate_data.py` 测试集循环内 L69 生成 `test_missing_indices`、L71 赋 NaN 使用外层残留变量 `missing_indices` 的不一致问题确实存在于脚本文本；**但对官方 release 实测未生效**——test 各缺失数块内缺失位置逐轨迹变化，且 k 帧缺失恰好呈现 C(8,k) 种位置组合（k=1→8 种、k=2→28、k=3→56、k=4→70），说明发布数据由正确版本逻辑生成。裁定：正式实验以发布 pkl 为准，不重新运行该脚本生成数据；若复现实验需要重跑生成逻辑，须先建立本地修正补丁并单独记录差异，分别报告"官方脚本输出"与"修正版脚本输出"。

已由 release 审计回答（2026-09-07，详见 §八.0.2）：实际字段键名与 shape、train/val/test 划分与规模、Hard 缺失分布（4–7 帧，官方直接发布）、Easy/Hard 缺失位置多样性、每样本至少一个有效历史观测、跨 split 无精确 GT 重叠。

仍待核验：官方预测实验的聚合口径与外部评估实现细节（官方仓库无原生 evaluator，`test.py` 为外部模型代码拷贝）——在实现本文评测脚本时按官方论文与外部模型实现复核。

### 0.2 TrajImpute release 下载与数据审计记录（2026-09-07）

官方 release（README Google Drive 链接）已下载至 `~/TrajImpute/dataset/TrajImpute/`（约 238 MB），结构为 5 场景（ETH-M/HOTEL-M/UNIV-M/ZARA1-M/ZARA2-M）× 2 协议（Easy/Hard）× 3 split（train/val/test）共 30 个 pkl。全量审计结果（30/30 文件，0 违规）：

1. **字段与 shape**：字典 8 键与 §八.0.1 第 2 条一致；`obs_traj` float32 `[N,8,2]`（缺失处为 NaN），`pred_traj` float32 `[N,12,2]`，`missing_mask` bool `[N,8,2]`。
2. **掩码语义**：`missing_mask=True` 表示该坐标缺失；实测每个文件的 `obs_traj` NaN 计数与 `missing_mask.sum()` 严格相等（30/30 成立）。
3. **未来完整**：全部 30 文件 `pred_traj` NaN 计数为 0；未来轨迹保持监督目标。
4. **Easy 分布**：train/val 每轨迹缺失 0–4 帧近均匀（如 ETH-M/Easy train 计数直方图近似 1/5 each，总体均值约 25%）。
5. **Hard 分布**：每轨迹缺失恰为 4–7 帧之一（直方图仅在 4/5/6/7 有质量），总体均值约 68.7%≈(4+5+6+7)/4/8。
6. **test 块结构**：Easy test 为 5 个整块拼接（缺失数分别恰为 0/1/2/3/4），Hard test 为 4 个整块（缺失数恰为 4/5/6/7）；各块含该场景全部测试轨迹，`pred_traj` 跨块完全相同（10/10 场景×协议验证：逐块缺失计数严格精确）。各场景唯一测试轨迹数：ETH 181 / HOTEL 1053 / UNIV 24334 / ZARA1 2253 / ZARA2 5833。
7. **缺失位置多样性**：块内缺失位置逐轨迹变化，k 帧缺失呈现全部 C(8,k) 种位置组合（k=1→8、k=2→28、k=3→56、k=4→70）——§八.0.1 第 8 条的脚本变量残留问题未影响发布数据。
8. **样本保障**：全部 30 文件逐样本最大缺失帧数 ≤7，即每个样本至少保留 1 个有效历史观测（0 违规）。
9. **划分隔离**：train/val/test 的 `pred_traj` 精确坐标集合两两无交集（5 场景均验证，泄漏 = 0）。

train/val 规模（以 ETH-M 为例，Easy 与 Hard 同底层数据）：train 29,809 / val 5,349 / test 905（=181×5）；各场景规模详见审计脚本输出。`seq_start_end` 保留帧序列内行人分组信息。

以上审计仅为数据完整性与分布核验；坐标单位、世界系语义与 Social-GAN 预处理一致性按官方论文描述采用，评测聚合口径见 §八.0.1"仍待核验"。

### 1. 已有基础

已有 ETH/UCY 留出实验在相同数据协议和评测口径下比较了单向与双向 Mamba。8 帧观测、12 帧预测、`K = 20` 的结果如下：

| 结构 | 平均 minADE20 | 平均 minFDE20 |
| --- | ---: | ---: |
| DeMo 单向 Mamba | 0.232 | 0.389 |
| DeMo 双向 Mamba | 0.244 | 0.416 |

当前结果支持采用单向历史时序建模作为缺失历史研究的优先基础配置。具体时序算子不构成论文的独立创新，而是作为整体时空上下文编码器的一部分。详细记录见 [完整数据基线对比](../results/完整数据基线对比.md)。

当前结果还说明，后续方法实验需要一个可复现、可解释的共享骨干；M0（单向）即为该骨干，三环境结果已收口。基础模型与公开强基线之间的绝对差距是性能改进方向，但不是把未经验证的通用模块直接计入缺失感知贡献的理由。

### 1.1 当前阶段的执行顺序

1. **冻结 M0 基线**：三环境（Clean/Easy-direct/Hard-direct）结果已收口，作为滚动基线与所有后续对照的零点；
2. **方法层重构**：确定新候选版本（约束见 §五.0A），代码与选题文档同步更新；
3. **Hard-direct 退化分析**：按官方缺失等级（4–7 帧）报告退化率与分组分析；
4. **确认性复验**：多种子、direct 两协议、插补后预测外部参考与 SDD 扩展证据。

### 1.2 统一验收标准

方法层版本必须同时通过性能、稳定性和归因三道门槛。

#### 性能门槛

以 `minFDE20` 作为第一主指标（主链 K=20，§1.6），以 `minADE20`、`MR` 和 `b-minFDE20` 作为辅助指标。候选增强至少应满足以下之一：

- 在 Easy-direct 零缺失分组（m=0）上，`minFDE20` 和 `minADE20` 均不劣于当前基线，并在至少一个主指标上出现具有实际意义的改善；
- 主指标改善幅度有限，但同时降低 `MR` 或 `b-minFDE20`，说明增强改善了失败样本或最差预测，而不只是平均值波动；
- 零缺失分组近似持平，但在缺失分组（m=1–4）表现出稳定收益，且收益能够由新增信息解释。

不能将仅有一个指标变好、其他关键指标明显退化的版本直接定义为“方法版本有效”。

#### 稳定性门槛

首轮可以使用已有单种子结果进行筛选，但进入主链前应至少进行相同数据划分下的 3 个种子复验

#### 归因门槛

每次实验只改变一个主要因素，并记录以下内容：

- 基础结构差异；
- 输入特征差异；
- 训练目标和损失权重差异；
- 缺失掩码及数据协议差异；
- 解码方式、采样数和后处理差异。

如果一个版本同时修改结构、特征和训练策略，即使最终数值提升，也只能作为工程候选，不能作为论文中的因果消融结论。方法层实验还必须同时报告零缺失分组与缺失分组结果，避免出现“零缺失变强、缺失退化”却仍被直接定义为共享骨干升级的情况。

### 1.3 论文主结果的核心表

为了将基础模型增强与当前缺失历史选题有机结合，论文主结果按本文 direct 扩展协议组织，不与历史 v1/v2/v3 预实验结果、也不与官方插补后预测结果混排：

#### 表一：完整历史绝对性能（Clean = 本文 DeMo 复现，暂定 0.232/0.389；MoFlow 0.202/0.322 与 TrajImpute Table 3 Clean 行为外部参照）

该表的作用是把基础模型的绝对性能差距单独呈现出来，避免后续缺失感知模块即使相对提升，也被误读为已经解决了通用预测能力不足的问题。`M0` 若仍明显弱于外部强基线，应在论文中如实说明，并将贡献重点放在缺失条件建模和退化分析，而不是声称整体预测性能已经达到领域 SOTA。

#### 表二：Easy-direct 上的方法层版本增量

表二固定 `M0`，按方法层版本逐级比较（版本序列待重构，见 §五.0A）。每一行只能新增一个逻辑模块，同时报告相对于 `M0` 的变化量，而不仅是绝对值。这样才能清楚区分“骨干能力”与“方法机制带来的增益”。

若某一模块必须依赖前一模块的输出，应在表中标明累计结构和新增结构；不能把多个新增模块合并为一个版本后，再把全部收益归因于最先提出的模块。对于尚未通过确认性门槛的版本，可以先放在附录或暂不报告，不应为了形成完整版本序列而提前纳入主结论。

#### 表三：Hard-direct 上的高缺失鲁棒性与退化分析

表三在 Hard-direct（缺失 4–7 帧，官方 release 直接发布、分布见 §八.0.2）上报告各版本相对 Easy-direct 的性能退化率：

\[
\Delta_m = \frac{m_{missing}-m_{easy}}{m_{easy}}.
\]

对于误差类指标，退化率越低越好。基于直接预测扩展数据的样本分组分析（缺失帧数、最长连续缺口、最后有效观测间隔）可附于此表之后，但分组分析不构成 TrajImpute 官方协议，也不构成新的正式协议。

#### 表四：直接缺失预测与插补后预测基线的公平比较

表四在相同测试样本上比较两类路径，必须明确区分并分别标注口径：

```text
直接预测     ：缺失历史 + missing mask → 本文模型 → 未来预测
插补后预测   ：缺失历史 → 外部插补器 → 预测器 → 未来预测
```

本文直接缺失输入的 `minADE_K`/`minFDE_K` 与插补后预测的单轨 ADE/FDE 属于不同口径，不得混为同一可比数字，不得把二者的结果不加说明地放在同一个“官方 benchmark 数值”表中。若官方基准仅报单轨 ADE/FDE，应分别报告“TrajImpute 原论文可参考指标”与“本文多模态预测指标”。

### 1.6 指标与评价口径

三类指标不得互相替代：

- **TrajImpute 原论文可参考指标**：ADE/FDE，具体口径按其预测实验与外部模型实现核验（官方仓库无原生 evaluator，见 §八.0.1）。不声称 TrajImpute 官方 evaluator 原生支持本文的多模态指标。
- **本文多模态预测指标**：主指标族为三件套 `minFDE_K` + `b-minFDE_K` + `MR`（minFDE 看最优模态，b-minFDE 捕捉概率质量，MR 观测证据塌缩）；`minADE_K`、ADE@1/FDE@1、覆盖率与时间连续性作为辅助。**主链 K=20（2026-09-08 裁定，与 TrajImpute 官方预测协议对齐）**：全部正式实验（P0 起）统一 `num_modes=20`、指标名 minFDE20/minADE20/b-minFDE20，与官方插补后预测及 ETH/UCY 社区 K=20 惯例同口径，直接可比。K=6 仅为历史预实验口径，不再使用、不再续跑；历史 K=6 数字不与 K=20 主结果混排。
- **插补质量指标**：MAE、MSE、RMSE、MRE，仅用于插补器对照（外部参考），不用于替代本文未来预测指标。

历史 v1/v2/v3 的 `minFDE6` 结果只在历史预实验文档内部比较，不进入 direct 主表，也不与官方插补后预测数字并排。

### 1.7 当前阶段的最终决策规则

执行顺序固定为：

1. M0 三环境基线已冻结，作为滚动基线；
2. 方法层新候选在 Easy-direct 上验证，以缺失等级分组分析呈现机制差异；
3. 验证通过后进入 Hard-direct 高缺失鲁棒性实验；
4. 最后进行多种子、direct 两协议（Easy/Hard）、外部插补后预测参考与 SDD 扩展证据的完整比较。

如果复验后仍没有稳定的缺失感知收益，应把研究问题收敛为“缺失历史条件下的退化建模、协议分析与可靠性评估”，而不是继续堆叠模块。如果复验出现稳定收益，再进入更完整的耦合模型，并用表二和表三分别证明模块增量与缺失鲁棒性。

这种推进方式保留已有工作的价值和选题的连续性：M0 基线回答“模型本身是否可靠”；方法层版本检验新机制能否在可靠骨干上产生独立贡献。

### 2. 实验任务协议

**正式主协议（2026-09-13 修订）**：Easy-direct/Hard-direct 使用 TrajImpute 官方 Easy/Hard release pkl，均为直接预测，不经外部插补；缺失历史 + missing mask + 有效观测间隔 + 场景信息直接输入模型，预测完整未来轨迹。Clean = 原始 ETH/UCY 零篡改（本文 Clean 基线 = DeMo 复现，暂定 0.232/0.389，8/28 轮；原论文数字为外部参照）；自建 benchmark_v1 已移除。

**外部参考协议（TrajImpute 官方插补后预测）**：官方 Clean / Easy-impute / Hard-impute 结果（缺失历史经外部插补器恢复后预测）仅用于有限范围的外部对照，不与本文 direct 结果混入同一“官方 benchmark 数值”表。

**历史自定义协议（已归档，不作为待执行主计划）**：v1（帧 0–5 缺失、帧 6/7 恒可见）、v2_high（同保护规则、37.5%–75% 高缺失）、v3_noguard（帧 0–7 全窗口无保护、锚点回退最后可见帧）。这些协议用于历史预实验与问题定位，其结果不与 direct 主结果混合。缺失帧数、连续缺口、缺口位置等维度若重新引入，只能作为基于直接预测扩展数据的**分组分析或补充分析**，不能作为新的正式协议。

ETH/UCY 与 SDD 用于检验方法在不同场景、坐标尺度与行人运动模式下的稳定性。无地图设置不作为论文主叙事，只作为当前行人轨迹输入条件。

### 3. 分阶段推进

正式实验阶段与本文 direct 扩展协议对齐；历史 v1/v2/v3 预实验不再作为阶段路径：

| 阶段 | 研究问题 | 方法与对照 | 形成的证据 |
| --- | --- | --- | --- |
| P1 | 方法层新候选（待重构）在缺失条件下的机制收益 | **Easy-direct**：新候选 vs `M0`（含缺失等级分组） | 按 §八.1.2 三道门槛裁定 |
| P2 | 方法在官方高缺失设置下的鲁棒性与退化 | **Hard-direct**：按官方缺失等级退化分析、插补后预测外部参考、SDD 扩展 | 论文结论范围（表三/表四） |

外部参考（单列，不与 direct 主表混合）：

| 参考协议 | 用途 |
| --- | --- |
| TrajImpute 官方 Clean / Easy-impute / Hard-impute | 与 TrajImpute 原论文的插补后预测结果进行有限范围的外部参考 |

### 4. 基线与消融关系

建议将对照设计围绕研究假设组织，而不是按网络组件堆叠：

| 对照组 | 回答的问题 |
| --- | --- |
| Easy m=0（零缺失）分组 `M0` | 零缺失条件下的基准 |
| 缺失历史 + 原始输入处理 | 原始模型面对缺失的自然退化 |
| 缺失历史 + 零填充/前向填充 | 简单缺失处理是否已足够 |
| 插补后预测或插补预测联合方法 | 直接预测与显式插补路线的关系（表四范式级比较） |

为保证每个结果可归因，正式实验至少应保留以下三组比较；每个方法版本报告 train_adapt（Easy-direct 同分布）；zero-shot 线（Easy 训练→Hard 测试）由 P2 承担，机制假说预期在 Hard-direct 上最显形：

```text
缺失历史增益：  方法层版本 - M0
协议退化：      同一模型的 Easy-direct -> Hard-direct test
```

其中，缺失历史增益回答“新机制是否有效”，协议退化回答“模型在缺失程度变化下是否稳健”。两者必须使用相同的 fold、seed、训练数据边界、checkpoint 选择规则和评测口径；不能用 `M0` 与方法层版本的非配对结果直接声称缺失感知带来全部提升。

其结果保持为单种子探索性证据，可进入研究记录与方法筛选，但不作为论文主表中的确认性证据。

### 5. 评价维度

| 维度 | 评价内容 |
| --- | --- |
| 预测准确性 | ADE、FDE、minADE、minFDE、Miss Rate |
| 缺失鲁棒性 | 不同缺失比例、连续长度、缺口位置下的性能曲线 |
| 多模态覆盖 | 最优候选命中、终点/方向多样性、合理未来分支覆盖 |
| 时间连续性 | 速度、加速度、曲率、转向变化的合理性与稳定性 |
| 概率与不确定性 | 模态概率、尺度、覆盖率、Energy Score 或校准指标 |
| 跨场景泛化 | ETH/UCY 不同场景与 SDD 上的趋势一致性 |
| 定性案例 | 转弯、减速、停走、避让、长连续遮挡后的未来预测 |

内部表示诊断可以辅助解释模型行为，但不应替代未来轨迹上的任务评价。论文结论应以缺失历史条件下的未来预测、覆盖性和连续性证据为主。

### 6. Go / No-Go 判断标准

**Go 条件**

- 缺失感知历史表征相对于零填充、简单掩码或未适配编码带来稳定收益；
- 缺失条件进入结构化未来分布后，除误差外还能改善模态覆盖或时间连续性中的至少一项；
- 收益在 Hard-direct 高缺失扩展评估中仍可观察到，而非只存在于 Easy-direct 轻度缺失；
- 主结果能够在 TrajImpute 官方五场景的多数子集上复现一致趋势，SDD 扩展证据方向不冲突；
- 零缺失分组（m=0）性能不发生不可接受的退化。

**No-Go 信号**

- 基础模型增强与缺失感知增强同时引入，导致无法分离两者的贡献；
- 简单填充或单纯拼接掩码已解释全部收益；
- 缺失感知历史编码无效，而复杂未来结构只增加参数量；
- 方法仅改善内部表征诊断，不改善真实未来预测、覆盖性或连续性；
- 收益只出现于 Easy-direct 或单一数据场景，Hard-direct 下方向反转；
- 未适配 DeMo 在所有缺失条件下已达到相同效果。

若出现 No-Go 信号，应据证据收缩论文主张：可能转向“缺失历史下 DeMo 的系统性评估与失败边界”，或重新定位新增方法的有效作用位置。

## 九、研究范围与待确定事项

### 1. 当前范围

```text
任务对象      : 行人历史轨迹缺失条件下的多模态未来预测
数据基准来源  : Easy/Hard-direct 使用 TrajImpute 官方 release pkl；Clean = 本文 DeMo 复现（暂定 0.232/0.389）
本文评估协议  : Easy-direct / Hard-direct（直接缺失预测；Clean = 本文复现基线）
外部参考协议  : TrajImpute 官方 Clean / Easy-impute / Hard-impute（插补后预测）
扩展验证数据  : SDD（跨数据集扩展证据；旧自定义缺失结果不构成正式主结果）
基础框架      : DeMo 的结构化多模态未来建模
共享骨干      : M0（原始 DeMo + 掩码拼输入，三环境全测）
缺失编码层    : 缺失观测感知的行人时空上下文编码器
核心新增方向  : 缺失感知历史表征 + 缺失条件化结构化未来分布
主要评价      : 预测准确性、模态覆盖、时间连续性、缺失鲁棒性
```

历史 v1/v2/v3 自定义协议及其结果文档已删除（2026-09-13），结论并入本文档 §八.0 统一标注；不作为正式主实验协议。

### 2. 暂不作为第一版主线的内容

```text
完整轨迹插补作为最终目标
直接解释隐藏向量为真实心理意图或物理状态
地图信息建模
定位噪声、误检、身份切换的联合退化建模
同时复现多个完整外部模型
```

这些方向可作为对照、扩展或后续工作，但不应稀释当前“缺失历史条件下的结构化多模态行人轨迹预测”主线。

### 3. 后续需要通过实验确定的事项

- 缺失条件最有效的表示是掩码、时间间隔、有效运动片段，还是三者的组合；
- 缺失条件应只影响历史编码，还是还应作用于 Mode/State Query 与 Hybrid Coupling；
- 是否需要显式插补分支、完整历史教师或运动学辅助约束；
- 不同缺失类型的训练测试分布应如何组织，才能同时反映适应性与泛化性；
- 最终采用的模态数量、缺失难度、概率评价指标和统计检验口径。

历史 v1/v2/v3 自定义协议及其全部结果文档已删除（2026-09-13）。当前正式协议见 §八.0。

## 参考文献

[^1]: Zhang, B., Song, N., and Zhang, L. (2024). “DeMo: Decoupling Motion Forecasting into Directional Intentions and Dynamic States.” *Advances in Neural Information Processing Systems 37*. DOI: 10.52202/079017-3383

[^2]: Chib, P. S., Nath, A., Kabra, P., Gupta, I., and Singh, P. (2024). “MS-TIP: Imputation Aware Pedestrian Trajectory Prediction.” *Proceedings of the 41st International Conference on Machine Learning*, PMLR 235, 8389–8402. https://proceedings.mlr.press/v235/chib24a.html

[^3]: Chib, P. S., and Singh, P. (2024). “Pedestrian Trajectory Prediction with Missing Data: Datasets, Imputation, and Benchmarking.” *Advances in Neural Information Processing Systems 37*. DOI: 10.52202/079017-3956

[^4]: Sun, C., Karlsson, P., Wu, J., Tenenbaum, J. B., and Murphy, K. (2019). “Stochastic Prediction of Multi-Agent Interactions from Partial Observations.” *International Conference on Learning Representations (ICLR)*.

[^5]: Xu, Y., Bazarjani, A., Chi, H.-G., Choi, C., and Fu, Y. (2023). “Uncovering the Missing Pattern: Unified Framework Towards Trajectory Imputation and Prediction.” *CVPR*, 9632–9643. DOI: 10.1109/CVPR52729.2023.00929

[^6]: Sun, J., Li, Y., Chai, L., Fang, H.-S., Li, Y.-L., and Lu, C. (2022). “Human Trajectory Prediction with Momentary Observation.” *CVPR*. DOI: 10.1109/CVPR52688.2022.00636

[^7]: Monti, A., Porrello, A., Calderara, S., Coscia, P., Ballan, L., and Cucchiara, R. (2022). “How Many Observations Are Enough? Knowledge Distillation for Trajectory Forecasting.” *CVPR*. DOI: 10.1109/CVPR52688.2022.00644

[^8]: Gu, T., Chen, G., Li, J., Lin, C., Rao, Y., Zhou, J., and Lu, J. (2022). “Stochastic Trajectory Prediction via Motion Indeterminacy Diffusion.” *CVPR*. DOI: 10.1109/CVPR52688.2022.01660

[^9]: Xu, P., Hayet, J.-B., and Karamouzas, I. (2022). “SocialVAE: Human Trajectory Prediction Using Timewise Latents.” *ECCV*. DOI: 10.1007/978-3-031-19772-7_30

[^10]: Chen, G., Li, J., Zhou, N., Ren, L., and Lu, J. (2021). “Personalized Trajectory Prediction via Distribution Discrimination.” *ICCV*. DOI: 10.1109/ICCV48922.2021.01529

[^11]: Bae, I., Park, Y.-J., and Jeon, H.-G. (2024). “SingularTrajectory: Universal Trajectory Predictor Using Diffusion Model.” *CVPR*. https://openaccess.thecvf.com/content/CVPR2024/html/Bae_SingularTrajectory_Universal_Trajectory_Predictor_Using_Diffusion_Model_CVPR_2024_paper.html

[^12]: Guo, D., Mordan, T., and Alahi, A. (2022). “Pedestrian Stop and Go Forecasting with Hybrid Feature Fusion.” *International Conference on Robotics and Automation (ICRA)*. DOI: 10.1109/ICRA46639.2022.9811664

[^13]: Zhang, Z., et al. (2024). “SocialCircle: Learning the Angle-based Social Interaction Representation for Pedestrian Trajectory Prediction.” *CVPR*. DOI: 10.1109/CVPR52733.2024.01798

[^14]: Cao, W., Wang, D., Li, J., Zhou, H., Li, L., and Li, Y. (2018). “BRITS: Bidirectional Recurrent Imputation for Time Series.” *Advances in Neural Information Processing Systems 31*.

[^15]: Shi, S., Jiang, L., Dai, D., and Schiele, B. (2022). “Motion Transformer with Global Intention Localization and Local Movement Refinement.” *Advances in Neural Information Processing Systems 35*.

_设计决策的文献支撑边界（2026-09-10 核验）：①“缺失下插补恢复的运动学量不可靠”——[^3]（TrajImpute 论文 Hard 条件下各基线集体大幅退化）；②“手工交互特征与学习式交互模块（Scene Context Transformer）重叠”——社区趋势性共识（Social-LSTM 以来的注意力交互演化），无专门对照研究；③Easy ≥2% 增益阈值与 Hard 不进筛选的决策规则——本文工程约定，无文献，P5 退化分析兜底。_

_本文档是当前选题、模型 idea、论文框架与实验推进的主记录。_
