name: sdd_missing
phase: actor-only

target:
  _target_: src.datamodule.sdd_missing_datamodule.SddMissingDataModule

  # missing_history 数据集：按 condition 读取 data/SDD_missing_v1/<condition>/{train,val,test}
  # v2 高缺失条件（random_fixed3/4、random_block3/4/6）把 data_root 指向
  # data/SDD_missing_v2_high 即可，目录结构相同（方案 §4.2）。
  data_root: '/home/lbh/DeMo/data/SDD_missing_v1'
  condition: complete   # complete | random_single | random_block2 | random_fixed3/4/5 | random_block3/4/6
  obs_len: 8
  pred_len: 12
  train_batch_size: ${batch_size}
  val_batch_size: ${batch_size}
  test_batch_size: ${batch_size}
  num_workers: ${num_workers}
  pin_memory: True
